<!-- templates/base.html -->
<!doctype html>
<html lang="id">
<head>
  <meta charset="utf-8">
  <title>{% block title %}Barcode Price App{% endblock %}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <!-- CSS sederhana -->
  <style>
    body { font-family: system-ui, Arial, sans-serif; margin: 0; background:#f6f7fb; color:#222;}
    header { background:#111827; color:#fff; padding:12px 16px; display:flex; justify-content:space-between; align-items:center;}
    main { padding:16px; max-width:1100px; margin:0 auto;}
    a { color:#0f62fe; text-decoration:none; }
    .btn { display:inline-block; padding:8px 12px; border-radius:8px; background:#0f62fe; color:#fff; }
    .btn.secondary { background:#6b7280; }
    .card { background:#fff; border:1px solid #e5e7eb; border-radius:12px; padding:16px; box-shadow: 0 1px 2px rgba(0,0,0,.04); }
    table { width:100%; border-collapse: collapse; }
    th, td { text-align:left; padding:8px 10px; border-bottom:1px solid #eee; }
    th { background:#f3f4f6; }
    .flash { padding:10px 12px; border-radius:8px; margin:8px 0; }
    .flash.success { background:#e6f4ea; color:#1e7e34; }
    .flash.danger { background:#fdecea; color:#c0392b; }
    .flash.info { background:#e8f0fe; color:#1a73e8; }
  </style>
  {% block head %}{% endblock %}
</head>
<body>
  <header>
    <div><strong>Barcode Price App</strong></div>
    <nav>
      {% if current_user.is_authenticated %}
        <span style="margin-right:12px;">Hi, {{ current_user.username }}</span>
        <a class="btn secondary" href="{{ url_for('logout') }}">Logout</a>
      {% endif %}
    </nav>
  </header>

  <main>
    {% with messages = get_flashed_messages(with_categories=true) %}
      {% if messages %}
        {% for category, message in messages %}
          <div class="flash {{ category }}">{{ message }}</div>
        {% endfor %}
      {% endif %}
    {% endwith %}

    {% block content %}{% endblock %}
  </main>
</body>
</html>